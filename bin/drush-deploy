#!/usr/bin/env ruby

require 'capistrano/cli'
require 'drush_deploy/paths'
args = ARGV
args = ['--file', DrushDeploy::Paths.recipe('bootstrap')] + args

cli = Capistrano::CLI.parse(args)
if cli.options[:actions].empty?
  args << 'deploy'
  cli = Capistrano::CLI.parse(args)
end
cli.execute!
